<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.HiltCode" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/dark_primary</item>
        <item name="colorAccent">@color/dark_accent</item>
        <item name="colorPrimaryVariant">@color/dark_variant</item>

        <!-- Customize your theme here. -->
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:background">@color/dark_primary</item>
        <item name="android:navigationBarColor">@color/dark_primary</item>
    </style>
</resources>